<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Receive text shares from other apps (Android share sheet → /chat/share picker).
                 text/plain covers URLs and selected text. Media/files (EXTRA_STREAM) are a future
                 additive intent-filter; this one is text-only for v1. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Verified App Links for cordn.net /chat (group shares) and /p (profile shares).
                 autoVerify=true plus the assetlinks.json at cordn.net/.well-known/ (served from
                 static/.well-known/) bind this APK's signing key to the domain, so Android opens
                 Cordn directly — no chooser, no browser — for matching paths; the appUrlOpen listener
                 (nativeBridge.ts) then routes the path in-app. pathPrefix scopes capture so
                 non-app paths (e.g. /blog, landing /) stay in the browser. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="cordn.net" android:pathPrefix="/chat" />
                <data android:scheme="https" android:host="cordn.net" android:pathPrefix="/p" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Camera is requested by the web QR scanner (getUserMedia) when the user opens a scan dialog.
         Capacitor's BridgeWebChromeClient bridges the WebView's onPermissionRequest to the Android
         runtime CAMERA prompt, but only if the permission is declared here — without it the launcher
         silently denies and the scanner reports "no camera". required=false so devices without a
         camera can still install. -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
</manifest>
